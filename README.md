# Тестовое задание на позицию Frontend-разработчика

Этот проект представляет собой двухстраничный веб-сайт с главной страницей, страницей контактов и формой обратной связи.

## Демо

Рабочую версию проекта можно посмотреть по ссылке: [Ссылка на демо](https://example.com)

## Функциональность

- Главная страница с встроенным YouTube видео, использующим фасад для улучшения LCP
- Страница контактов с формой обратной связи
- Валидация полей формы
- Адаптивный дизайн для мобильных и десктопных устройств
- Обработка отправки формы с отображением результата

## Технологии

- React 19 + TypeScript
- Material UI для компонентов пользовательского интерфейса
- Emotion для CSS-in-JS стилизации
- Vite для сборки и разработки
- MSW (Mock Service Worker) для эмуляции API
- React Router для маршрутизации
- React Helmet для управления мета-тегами
- Axios для HTTP-запросов

## Установка и запуск

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

### Сборка для продакшна

```bash
npm run build
```

### Предпросмотр сборки

```bash
npm run preview
```

## Развертывание проекта (Деплой)

### Подготовка к деплою

Перед развертыванием проекта убедитесь, что:

1. Проект успешно собирается без ошибок: `npm run build`
2. Все тесты проходят успешно (если они есть): `npm test`
3. Все переменные окружения правильно настроены (при необходимости)

### Деплой на Netlify

Netlify предлагает простой и быстрый способ развертывания React приложений:

1. Создайте аккаунт на [Netlify](https://www.netlify.com/)
2. Установите Netlify CLI: `npm install -g netlify-cli`
3. Авторизуйтесь: `netlify login`
4. Соберите проект: `npm run build`
5. Разверните проект: `netlify deploy --prod`

Или через интерфейс Netlify:

1. Загрузите код на GitHub/GitLab/Bitbucket
2. В панели управления Netlify нажмите "New site from Git"
3. Выберите репозиторий
4. Настройте:
   - Build command: `npm run build`
   - Publish directory: `dist`
5. Нажмите "Deploy site"

### Деплой на Vercel

Vercel специализируется на хостинге React приложений:

1. Создайте аккаунт на [Vercel](https://vercel.com/)
2. Установите Vercel CLI: `npm install -g vercel`
3. Авторизуйтесь: `vercel login`
4. Разверните проект: `vercel --prod`

Или через интерфейс Vercel:

1. Загрузите код на GitHub/GitLab/Bitbucket
2. Импортируйте проект из репозитория
3. Настройте:
   - Framework Preset: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
4. Нажмите "Deploy"

### Деплой на Firebase Hosting

1. Создайте проект Firebase на [Firebase Console](https://console.firebase.google.com/)
2. Установите Firebase CLI: `npm install -g firebase-tools`
3. Авторизуйтесь: `firebase login`
4. Инициализируйте Firebase в проекте: `firebase init hosting`
   - Выберите ваш проект Firebase
   - Укажите `dist` как публичную директорию
   - Настройте SPA (ответьте "yes" на вопрос о редиректе)
5. Соберите проект: `npm run build`
6. Разверните проект: `firebase deploy --only hosting`

### Деплой на GitHub Pages

1. Установите пакет gh-pages: `npm install --save-dev gh-pages`
2. Добавьте в `package.json`:
   ```json
   "homepage": "https://ваше-имя.github.io/имя-репозитория",
   "scripts": {
     "predeploy": "npm run build",
     "deploy": "gh-pages -d dist"
   }
   ```
3. Создайте файл `vite.config.js` (или отредактируйте существующий):
   ```js
   export default {
     base: "/имя-репозитория/",
   };
   ```
4. Разверните проект: `npm run deploy`

## Особенности реализации

### Стилизация и CSS

В проекте используется библиотека Emotion для CSS-in-JS. Это позволяет:

1. Создавать стилизованные компоненты с динамическими стилями
2. Избегать конфликтов имен классов
3. Повысить переиспользуемость компонентов

**Пример использования Emotion:**

```tsx
const StyledButton = styled(Button)`
  color: #1976d2;
  font-weight: 600;
  &:hover {
    background-color: rgba(25, 118, 210, 0.04);
  }
`;
```

### Продвинутая CSS-техника

В проекте реализован кастомный фасад для YouTube-видео, который значительно улучшает LCP (Largest Contentful Paint). Вместо загрузки тяжелого iframe, сначала отображается легкая статическая картинка с превью и кнопкой воспроизведения, и только при нажатии загружается сам плеер.

Компонент YouTubeFacade обеспечивает:

- Ленивую загрузку видео только при клике
- Интеллектуальную загрузку превью с резервными вариантами
- Адаптивный дизайн сохраняющий соотношение сторон 16:9
- Красивый индикатор загрузки во время получения превью
- Кэширование загруженных превью для улучшения производительности

### SEO и производительность

- Все страницы содержат правильную структуру заголовков (h1, h2, ...)
- Добавлены мета-теги для Open Graph и Twitter Cards
- Изображения имеют атрибут alt для доступности
- Используется ленивая загрузка для улучшения производительности
- YouTube видео загружается по требованию, значительно улучшая LCP

### Бэкенд и API

Для эмуляции бэкенда используется Mock Service Worker (MSW). В реальном проекте MSW можно заменить на настоящий серверный API.

MSW настроен со следующими особенностями:

- Обработка запросов только к локальным API
- Пропуск внешних запросов (например, к YouTube) для предотвращения проблем с CORS
- Эмуляция задержки сети для более реалистичного поведения
- Валидация данных формы на стороне "сервера"

При отправке формы данные логируются в консоль, и возвращается сообщение с именем пользователя, как указано в требованиях.

## Устранение проблем

### Проблемы с MSW

Если вы столкнулись с ошибками CORS при загрузке внешних ресурсов:

1. Проверьте настройки в `src/mocks/browser.ts` - убедитесь, что внешние запросы не перехватываются
2. При необходимости перезапустите приложение: `npm run dev`

### Проблемы с отображением YouTube превью

Если превью YouTube не загружаются:

1. Проверьте сетевые запросы в DevTools браузера
2. Убедитесь, что компонент `YouTubeFacade` пробует различные форматы превью
3. Проверьте, что MSW правильно пропускает запросы к домену youtube.com

## Хостинг

Проект может быть размещен на:

- Netlify: удобный деплой из GitHub, бесплатный план
- Vercel: специализированный хостинг для React приложений
- GitHub Pages: с настройкой для SPA
- Firebase Hosting: для интеграции с другими сервисами Firebase

## Предположения и решения

1. Для улучшения UX добавлена валидация формы как на стороне клиента, так и возможность обработки ошибок от сервера
2. Добавлены анимации для улучшения взаимодействия с пользователем
3. Страницы сделаны адаптивными, с разным расположением элементов на мобильных устройствах
4. Добавлен полноценный футер для улучшения навигации
5. Реализована устойчивая обработка ошибок при загрузке внешних ресурсов

## Дополнительные возможности для будущего развития

- Добавление анимаций при переходах между страницами
- Интеграция с настоящим бэкендом
- Добавление дополнительных страниц (О нас, Портфолио, и т.д.)
- Многоязычность через i18n
- Темная тема
- Прогрессивное веб-приложение (PWA)
- Улучшенное тестовое покрытие
